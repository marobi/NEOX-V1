MEMORY {

    ZP:     start = $0000, size = $0001, type = rw;

    BIOS:   start = $F000, size = $1000, type = ro, file = %O;
}

SEGMENTS {
    ZEROPAGE:   load = ZP,   type = zp;

    BIOS:       load = BIOS, type = ro;
}
