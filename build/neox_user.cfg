MEMORY {
     USER:   start = $2000, size = $6000, type = rw, file = %O;
}

SEGMENTS {
    USER_ENTRY: load = USER, type = ro;
    USER_TEXT:  load = USER, type = ro;
    USER_DATA:  load = USER, type = rw;
}
