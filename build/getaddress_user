#!/bin/sh
#
# Get load and run addresses from label file and output in hex in format
# to be used as arguments to the bintomon program.

LOAD=`grep ".user_image_header" ./out/neox_user.lbl |  awk '{ print $2 }' | head -1`
echo "-l 0x${LOAD}"
